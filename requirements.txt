# Core Python version (optional; set via pyenv or virtualenv)
# python==3.7.13
# pip==22.3.1

# CUDA and PyTorch packages
torch # ==1.12.1+cu116
torchvision # ==0.13.1+cu116
torchaudio #==0.12.1+cu116
# Note: If pip doesn’t recognize "+cu116" build, use the official PyTorch wheel URL instead (see note below)

# Other dependencies
plyfile # ==0.8.1
tqdm
opencv-python #==4.8.1.78
munch
trimesh
evo ==1.11.0
open3d #==0.17.0
torchmetrics
imgviz
PyOpenGL
glfw
PyGLM
wandb
lpips
rich
ruff

